import { createTheme } from "@mui/material/styles"

const theme = createTheme({
    palette: {
        primary: {
            main: "#1976d2",
        },
        // Customize other palette colors or typography as needed
    },
})

export default theme
